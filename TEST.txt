// Basic while statement test
main
var a, b, c, g, d;
{
	if a < b then
		let a <- a + 5;

		if a < b then
		    call d;
		    call g
		fi

	else
	    let b <- g;
	    let b <- g;
	    let b <- g;
	    let b <- g;
	    let b <- g
	fi
}.